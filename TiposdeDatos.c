#include <stdio.h>

int main (int argc, char *argv[]) {
    
    //int: 0, -15, 1956    ; valores enteros

    /* Ambos sirven para lo mismo caracterización de valores reales, 
    pero se diferencian en la cantidad de espacio con la cual se guardan. 
    float: 5.34, 8.97, -100.5   ; 32 bits
    double: 1000.653, -200.65   ; 64 bits
    */
    //char: 't', 'a', '9', '\n'   ; Todo lo que esta dentro de las comillas simples se vuelven en caracteres

    int numero_entero = 16; 
    float numero_real_simple = 5.16;
    double numero_real_doble = 1000.4567;
    char caracter = 't'; 
    
    return 0;
}